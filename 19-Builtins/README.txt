Java built in heap
     Priority Queue 

hashmap implements map interface, hashset implements set interface. hashmap keys to values, hashset stores a set of data.

hashmap is a faster but unsorted tree map
